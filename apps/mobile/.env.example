# Local env — mobile (copy to .env)
# Find your LAN IP: ipconfig (Windows) -> IPv4, or ipconfig getifaddr en0 (macOS)
EXPO_PUBLIC_API_URL=http://192.168.0.10:3333
# Optional (Google Maps on Android standalone / dev client only; not needed for Expo Go):
# EXPO_PUBLIC_GOOGLE_MAPS_KEY=...
